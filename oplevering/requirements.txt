numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13
networkx>=3.0
pyyaml>=6.0
# Notebook support: ipykernel (+ optional nbconvert for CLI execute).
# Avoid `pip install jupyter` on Windows if paths are long (jupyterlab paths can fail).
ipykernel>=6.0
nbconvert>=7.0
ipympl>=0.9.3
